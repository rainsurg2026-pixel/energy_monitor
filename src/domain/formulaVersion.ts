/**
 * Formula contract used by the Phase 1 golden parity fixtures.
 *
 * This deliberately names the Desktop release whose behavior is being
 * preserved. It is not an application version or a migration target.
 */
export const DESKTOP_FORMULA_VERSION = "desktop-v2.3.1" as const;
