/** Compatibility adapter for legacy callers; presentation-only exports. */
export { formatNumber, formatDecimal, formatPercentage, formatEnergy, formatCompactNumber, formatCompactLabel, formatFixedNumber, formatFixedPercentage, formatGWh } from "./numberFormat";
import { formatNumber } from "./numberFormat";

export const formatNumber2 = formatNumber;
