---
type: mobile-blueprint
app_name: <ชื่อ app>
platform: iOS / Android / both
stack: React Native / Flutter / Swift / Kotlin
created: YYYY-MM-DD
---

# Mobile Blueprint: <ชื่อ app>

## Overview

| Field | Value |
|-------|-------|
| App name | ... |
| Platform | iOS + Android |
| Stack | Expo + Zustand + Supabase |
| Target audience | ... |
| Core feature | ... (3-5 bullets) |
| MVP timeline | X สัปดาห์ |

---

## Stack Decision

**เลือก:** <stack name>

**เหตุผล:**
1. ...
2. ...
3. ...

**Alternative ที่พิจารณา:**
- <stack A> — ข้อดี/ข้อเสีย
- <stack B> — ข้อดี/ข้อเสีย

---

## Project Structure

```
my-app/
├── app/
├── components/
├── hooks/
├── services/
├── store/
├── types/
└── ...
```

---

## Core Features (พร้อมโค้ด)

### Feature 1: Authentication
```tsx
// services/auth.ts
...
```

### Feature 2: ...
```tsx
...
```

---

## State Management

**ใช้:** Zustand / Redux Toolkit / Provider / Riverpod

```tsx
// store/index.ts
...
```

---

## Backend Integration

| API | Endpoint | Auth |
|-----|----------|------|
| Login | POST /auth/login | none |
| Get user | GET /users/me | bearer |

---

## Permission + Privacy

### iOS (Info.plist)
```xml
<key>NSCameraUsageDescription</key>
<string>เพื่อถ่ายรูป profile</string>
```

### Android (AndroidManifest.xml)
```xml
<uses-permission android:name="android.permission.CAMERA" />
```

### Privacy Manifest (iOS 17+)
- Data category: ...
- Reason: ...

---

## Build & Deploy

### Setup EAS
```bash
npm install -g eas-cli
eas build:configure
```

### Build
```bash
eas build --platform ios --profile production
eas build --platform android --profile production
```

### Submit
```bash
eas submit --platform ios
eas submit --platform android
```

---

## Pre-launch Checklist

- [ ] Test บนเครื่องจริงทั้ง iOS + Android
- [ ] TestFlight 5+ tester ผ่าน 7 วัน
- [ ] Crash rate < 1% ใน Sentry
- [ ] App size < 100MB (iOS over-the-air limit)
- [ ] Icon + splash + screenshot ครบทุก size
- [ ] Privacy policy + terms URL active
- [ ] App Store Connect / Play Console กรอกครบ

---

## Cost Estimate

- Apple Developer: $99/ปี
- Google Play: $25 (one-time)
- Backend (Supabase free → Pro): $0-25/เดือน
- Sentry (crash report): free tier

---

*Generated by /mobile-app-builder — Claude Skill Unlock v1.1*
