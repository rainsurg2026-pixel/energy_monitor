---
type: ai-blueprint
project: <ชื่อ feature>
model_primary: claude-sonnet-4-5 / gpt-4o
use_case: chatbot / RAG / extraction / classification
created: YYYY-MM-DD
---

# AI Blueprint: <Feature>

## Overview

| Field | Value |
|-------|-------|
| Feature | ... |
| Use case | RAG chatbot for support |
| Primary model | claude-sonnet-4-5 |
| Fallback model | claude-haiku-4 |
| Vector DB | pgvector |
| Expected traffic | 10K calls/day |
| Cost target | < $500/เดือน |

---

## Model Selection

**Primary:** <model>
**เหตุผล:**
- ตอบไทยดี
- รองรับ prompt caching
- Cost balance ดี

**Fallback:** <model> สำหรับ task ง่าย

**Cost analysis:**
```
Daily: 10K calls × (500 in + 300 out) tokens
       × ($3/M in + $15/M out) = $60/day
+ system prompt cached 90%: -$25
= ~$35/day = $1,050/เดือน
```

---

## Architecture

```
User → Next.js API → [Embed] → pgvector retrieve top-5
                  → [Rerank] → top-3 chunks
                  → [LLM] Claude Sonnet stream → User
```

---

## Data Pipeline

### Ingestion
1. ดึงข้อมูลจาก <source>
2. Clean + extract text
3. Chunk 800 tokens / 100 overlap
4. Embed ด้วย `text-embedding-3-small`
5. Upsert ลง pgvector

```python
# scripts/ingest.py
...
```

### Retrieval
```sql
SELECT id, content, 1 - (embedding <=> $1::vector) AS score
FROM chunks
WHERE source = $2
ORDER BY embedding <=> $1::vector
LIMIT 20;
```

---

## Prompt Design

### System Prompt (cached)
```
คุณเป็น support assistant สำหรับ <product>
ตอบจาก context เท่านั้น ถ้าไม่รู้ตอบ "ไม่พบข้อมูล กรุณาติดต่อ support@..."
ตอบเป็นภาษาไทย กระชับ < 200 คำ
อ้าง source ทุกคำตอบ [Source: doc-id]
```

### User Prompt (per request)
```
Context:
{{retrieved_chunks}}

Question: {{user_question}}
```

---

## Code

```typescript
// app/api/chat/route.ts
...
```

---

## Token Economics

| Component | Per call | Daily (10K calls) |
|-----------|----------|-------------------|
| System prompt (cached) | 2K tokens | $0.30 read |
| Retrieved context | 2K tokens | $6 |
| User input | 100 tokens | $0.30 |
| Output | 300 tokens | $4.50 |
| Embedding | — | $0.20 |
| **Total** | — | **~$11/day** |

**Optimization:**
- [ ] Prompt caching (90% off system)
- [ ] Semantic cache (Redis) — duplicate question → cached answer
- [ ] Switch to Haiku for "is this in scope" check

---

## Monitoring

| Metric | Threshold | Tool |
|--------|-----------|------|
| Latency p95 | > 3s | Datadog |
| Token cost / day | > $50 | Custom dashboard |
| Hallucination rate | sample 100/week | Manual review |
| Cache hit rate | < 30% | Redis stats |

---

## Safety

- [ ] Prompt injection: input sanitize + structured tags
- [ ] PII redaction before logging
- [ ] Rate limit: 60 req/min per user
- [ ] Output filter: regex secret/PII pattern
- [ ] Hallucination guard: refuse if no context match

---

## Rollout Plan

1. **Week 1:** Internal team test (10 user)
2. **Week 2:** 5% canary
3. **Week 3-4:** 50% rollout, A/B test cost vs satisfaction
4. **Month 2:** 100% + monitor

---

*Generated by /ai-engineer — Claude Skill Unlock v1.1*
