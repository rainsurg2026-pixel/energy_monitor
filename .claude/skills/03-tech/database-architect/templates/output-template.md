---
type: db-blueprint
project: <ชื่อ project>
db_engine: PostgreSQL 16 / MySQL 8 / MongoDB 7
created: YYYY-MM-DD
---

# Database Blueprint: <Project>

## Overview

| Field | Value |
|-------|-------|
| Engine | PostgreSQL 16 |
| Use case | OLTP — e-commerce |
| Expected size (1 yr) | 10M users, 100M orders |
| Read/write ratio | 80/20 |
| Replication | 1 primary + 2 read replica |
| Backup | daily snapshot + WAL archiving |

---

## Schema (DDL)

```sql
-- users
CREATE TABLE users (
  id BIGSERIAL PRIMARY KEY,
  email CITEXT UNIQUE NOT NULL,
  ...
);

-- products
CREATE TABLE products (...);

-- orders
CREATE TABLE orders (...);
```

### Entity Relationship

```
users (1) ─── (N) orders (1) ─── (N) order_items (N) ─── (1) products
```

---

## Index Plan

| Table | Index | Type | Purpose |
|-------|-------|------|---------|
| users | `(email)` | B-tree UNIQUE | login lookup |
| orders | `(user_id, status, created_at DESC)` | B-tree composite | dashboard query |
| products | `(search_vector)` | GIN | full-text search |
| products | `(metadata jsonb_path_ops)` | GIN | filter by attribute |

**ไม่ใส่:**
- `(created_at)` บน users — ใช้ไม่บ่อย
- ทุก foreign key ไม่จำเป็นต้อง index ถ้าไม่ JOIN

---

## Query Examples (พร้อม EXPLAIN)

### Q1: Order list ของ user (ใช้บ่อย)
```sql
EXPLAIN ANALYZE
SELECT id, status, total_cents, created_at
FROM orders
WHERE user_id = 123 AND status IN ('paid', 'shipped')
ORDER BY created_at DESC
LIMIT 20;
```

**Plan:** Index Scan using `orders_user_status_idx` — actual time 0.5ms ✅

### Q2: ...

---

## Migration Plan

### Phase 1: Initial setup
```sql
-- migrations/001_init.sql
...
```

### Phase 2: Add new column (zero-downtime)
```sql
-- Step 1: nullable
ALTER TABLE users ADD COLUMN phone TEXT;
-- Step 2: backfill (batch)
UPDATE users SET phone = '' WHERE phone IS NULL AND id BETWEEN 1 AND 100000;
-- Step 3: NOT NULL
ALTER TABLE users ALTER COLUMN phone SET NOT NULL;
```

---

## Backup Strategy

- **Daily:** `pg_dump` + upload to S3 (retain 30 days)
- **Hourly:** WAL archiving (PITR)
- **Weekly:** test restore to staging
- **Cross-region:** replicate snapshot (DR)

---

## Connection Pool

| Setting | Value | Reason |
|---------|-------|--------|
| `max_connections` | 200 | RAM 8GB / connection 4MB ≈ 200 |
| App pool size | 20 per instance | 10 instances × 20 = 200 |
| Idle timeout | 60s | ปล่อย slot ให้ instance อื่น |
| Statement timeout | 30s | กัน slow query loops |

---

## Performance Tuning

```ini
# postgresql.conf
shared_buffers = 2GB           # 25% RAM
effective_cache_size = 6GB     # 75% RAM
work_mem = 16MB                # per operation
maintenance_work_mem = 512MB
random_page_cost = 1.1         # SSD
```

---

## Monitoring

- Slow query log: `log_min_duration_statement = 1000`
- pgBadger weekly report
- Prometheus exporter: `pg_exporter`
- Alert: deadlock rate, replication lag, cache hit < 95%

---

## Disaster Recovery

| Scenario | RTO | RPO | Action |
|----------|-----|-----|--------|
| Primary crash | 5 นาที | 0 | promote replica |
| Data corruption | 1 ชม. | 1 ชม. | PITR to last good WAL |
| Region outage | 4 ชม. | 24 ชม. | restore latest snapshot in DR region |

---

*Generated by /database-architect — Claude Skill Unlock v1.1*
