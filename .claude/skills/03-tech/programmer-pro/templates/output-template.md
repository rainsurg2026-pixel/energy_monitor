---
type: code-review
language: <ภาษา>
framework: <framework ถ้ามี>
mode: review / debug / refactor / explain
created: YYYY-MM-DD
---

# Code Review: <หัวข้อ / ชื่อไฟล์>

## Summary

| Field | Value |
|-------|-------|
| Language | TypeScript |
| Framework | React 18 |
| Lines reviewed | 85 |
| Critical | 1 |
| Major | 2 |
| Minor | 3 |
| Verdict | Needs changes |

**TL;DR:** <2-3 ประโยค สรุปปัญหาสำคัญ>

---

## Critical Issues

### #1 <ชื่อปัญหา>

**Location:** `<ไฟล์>:<บรรทัด>`
**Category:** Bug / Security / Performance / Data loss
**Severity:** Critical

**Problem:**
> อธิบายปัญหา 2-3 ประโยค

**Impact:**
- ...
- ...

**Reproduction:**
```
1. ทำ X
2. เห็น Y
3. คาดหวัง Z
```

**Code (before):**
```<lang>
// บรรทัด X-Y
...
```

**Fix:**
```<lang>
...
```

**Why this works:**
> อธิบายสั้นๆ ว่าทำไม fix นี้แก้ปัญหา

---

## Major Issues

### #1 <ชื่อ>
**Location:** ...
**Problem:** ...
**Fix:**
```<lang>
...
```

---

## Minor Issues / Suggestions

- **Naming:** `data` ควรเป็น `userList` (L23)
- **Magic number:** `300` ควรเป็น `const DEBOUNCE_MS = 300` (L45)
- **Comment:** L67-70 ควรมี comment อธิบายทำไมใช้ setTimeout

---

## Refactored Code (ฉบับเต็ม)

```<lang>
// โค้ดใหม่ทั้งชุด พร้อม import
...
```

**เปลี่ยนแปลงสำคัญ:**
1. แยก logic ออกเป็น custom hook
2. ใช้ useMemo cache expensive calculation
3. เพิ่ม error boundary

---

## Test Cases ที่ควรเพิ่ม

```<lang>
describe('<feature>', () => {
  it('should handle empty array', () => {...})
  it('should handle network error', () => {...})
  it('should debounce search input', () => {...})
})
```

---

## Learning Notes

- หลักที่ใช้: <SOLID / DRY / YAGNI / etc.>
- Reference: <link เอกสาร MDN / official docs>
- Pattern: <ชื่อ pattern ถ้าใช้>

---

## Production Checklist

- [ ] Fix critical issues ทั้งหมด
- [ ] เพิ่ม test coverage อย่างน้อย happy path + 1 sad path
- [ ] Run linter / formatter
- [ ] Run existing test suite — ไม่มีอันไหนพัง
- [ ] Review breaking change — update changelog

---

*Generated by /programmer-pro — Claude Skill Unlock v1.0*
